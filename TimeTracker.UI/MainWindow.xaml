<Window x:Class="TimeTracker.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TimeTracker" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="200" Text="{Binding NewScheduleTitle, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Add Schedule" Command="{Binding AddScheduleCommand}" Margin="10,0,0,0" />
        </StackPanel>

        <ListBox Grid.Row="1" ItemsSource="{Binding Schedules}" DisplayMemberPath="Title" />
    </Grid>
</Window>